const panelShop = document.querySelector("#panelShop");
class Styles {
    constructor() {
        // window.addEventListener('scroll', (e) => {
        //     panelShop.style.marginTop = 50 - window.pageYOffset > 0 ? 50 - window.pageYOffset + 'px' : 0;

        // })
    }
}

export { Styles };
